---
name: te3-ui
description: Navigate Tabular Editor 3 through the FlaUI MCP tools and capture documentation screenshots. Use for TE3 desktop UI tasks, not DAX or model authoring.
---

This skill is assembled from `automation/skills/layers` by `build-skill.mjs`, one rung
at a time, as the ladder study in `automation/experiments/LADDER.md` earns each line.
Until that study fills the layers, it adds no TE3 guidance.
