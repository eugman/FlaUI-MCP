---
name: te3-ui
description: Navigate Tabular Editor 3 through FlaUI MCP and capture documentation screenshots using scoped TE3 guidance. Use for TE3 desktop UI tasks, not general DAX or model authoring.
---

Use `te3_catalog()` for the topic index, then `te3_catalog(topic:"TOPIC_ID")`
to read only the map section relevant to the task. Use topic IDs from the index.

For screenshot tasks, also read the `capture` topic.

If the catalog is unavailable and this repository is accessible, read
`automation/map/start.md`, then the relevant topic file. Do not install or
reconfigure tools just to retrieve guidance.
