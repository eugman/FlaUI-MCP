<!-- Condition 5 only: the TE3 tool exists only there. Five-condition study: code-actions-03 skipped te3_navigate (23 calls), and no object trial used its object destination. -->
- Use te3_navigate for its destinations instead of the manual steps above, including `object` with table, objectName and objectType.
