<!-- Rung 2: TE3 rules, steps and capture checklist. Add only lines that fix failures seen in at least two rung 1 training trials. Source pool: automation/skills/te3-notes. Never derive lines from the hold-out task. -->

<!-- rung 1 script-01, -02, -03: opened scripts through the File menu and its submenu (~44, ~23, ~11 wasted calls); 0c used the toolbar button in one call. -->
- To open a script, click the Button "Open..." on the main toolbar, not the File menu.

<!-- rung 1 script-02, -03: hunted for the run button through capped snapshots (~6, ~9 calls). -->
- Run the loaded script with Button "Run script"; find it by name with windows_find.

<!-- rung 1 formatting-01, -02, -03: first searches missed Preferences... or looked for a MenuItem. -->
- Preferences... is a Button in the Tools menu popup; find it by name with includeOwned:true.

<!-- rung 1 formatting-01, -02, -03: finds for "Text Editors" missed rows scrolled out of view (~1, ~5, ~4 calls). -->
- The Preferences tree only exposes rows scrolled into view: click a visible row and press PageDown until Text Editors appears.

<!-- rung 1 formatting-02, -03: probed scrollbars to judge whether the page fit (~3, ~2 calls). -->
- Before capturing Auto Formatting, size Preferences to 1155 × 714 with windows_place_window so every control fits.
